# EDGP Phase 3: 完善与部署 - 执行计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 完善模板系统（说说/友链/关于页）+ Git 推送命令，实现一键部署到 GitHub Pages

**Architecture:** 
- 新增 3 个页面模板：moments.html, friends.html, about.html
- 扩展 StaticSiteGenerator 生成这些页面
- 添加 `python manage.py push` 命令

**Tech Stack:** Django, Git, python-slugify

---

## Phase 3 任务概览

```
Phase 3.1: 缺失页面模板 (3 tasks)
Phase 3.2: 生成器扩展 (1 task)
Phase 3.3: push 命令实现 (1 task)
Phase 3.4: 最终验证 (1 task)
```

---

## Phase 3.1: 缺失页面模板

### Task 1: 创建 moments.html（说说页面）

**Files:**
- Create: `site_templates/pages/moments.html`

**Step 1: 创建 moments.html**

```html
{% extends 'base.html' %}

{% block title %}说说 - {{ site.title }}{% endblock %}

{% block content %}
<h1 class="mb-4">说说</h1>

{% for moment in moments %}
<article class="card mb-3">
    <div class="card-body">
        <div class="moment-content">
            {{ moment.content_html|safe }}
        </div>
        {% if moment.images %}
        <div class="moment-images mt-2">
            {% for image in moment.images %}
            <img src="{{ image }}" class="img-thumbnail me-2" style="max-height: 200px;">
            {% endfor %}
        </div>
        {% endif %}
        <p class="text-muted mt-2 mb-0">
            <small>{{ moment.created_at|date:"Y-m-d H:i" }}</small>
        </p>
    </div>
</article>
{% empty %}
<div class="alert alert-info">暂无说说</div>
{% endfor %}
{% endblock %}
```

**Step 2: Commit**

```bash
git add site_templates/pages/moments.html
git commit -m "feat: add moments page template"
```

---

### Task 2: 创建 friends.html（友链页面）

**Files:**
- Create: `site_templates/pages/friends.html`
- Create: `site_templates/components/friend_link.html`

**Step 1: 创建 components/friend_link.html**

```html
<div class="friend-link card mb-3">
    <div class="card-body d-flex align-items-center">
        <img src="{{ link.avatar }}" alt="{{ link.name }}" 
             class="rounded-circle me-3" 
             width="60" height="60"
             onerror="this.src='https://via.placeholder.com/60'">
        <div>
            <h5 class="mb-1">
                <a href="{{ link.url }}" target="_blank" class="text-decoration-none">
                    {{ link.name }}
                </a>
            </h5>
            <p class="text-muted mb-0 small">{{ link.description }}</p>
        </div>
    </div>
</div>
```

**Step 2: 创建 pages/friends.html**

```html
{% extends 'base.html' %}

{% block title %}友链 - {{ site.title }}{% endblock %}

{% block content %}
<h1 class="mb-4">友情链接</h1>

<div class="friend-links">
{% for link in links %}
    {% include 'components/friend_link.html' %}
{% empty %}
<div class="alert alert-info">暂无友链</div>
{% endfor %}
</div>

<hr class="my-4">

<p class="text-muted">
    欢迎交换友链！请通过 About 页面的联系方式与我联系。
</p>
{% endblock %}
```

**Step 3: Commit**

```bash
git add site_templates/pages/friends.html site_templates/components/friend_link.html
git commit -m "feat: add friends page template"
```

---

### Task 3: 创建 about.html（关于页面）

**Files:**
- Create: `site_templates/pages/about.html`

**Step 1: 创建 about.html**

```html
{% extends 'base.html' %}

{% block title %}关于 - {{ site.title }}{% endblock %}

{% block content %}
<div class="about-page">
    <div class="text-center mb-4">
        <h1>关于我</h1>
    </div>
    
    <div class="about-content">
        {{ about_content|safe|default:"暂无介绍" }}
    </div>
    
    <hr class="my-4">
    
    <h3>联系方式</h3>
    <ul class="list-unstyled">
        {% if site.author %}
        <li><strong>作者：</strong> {{ site.author }}</li>
        {% endif %}
        {% if site.url %}
        <li><strong>网站：</strong> <a href="{{ site.url }}">{{ site.url }}</a></li>
        {% endif %}
    </ul>
</div>
{% endblock %}
```

**Step 2: Commit**

```bash
git add site_templates/pages/about.html
git commit -m "feat: add about page template"
```

---

## Phase 3.2: 生成器扩展

### Task 4: 扩展 StaticSiteGenerator

**Files:**
- Modify: `blog/utils/generator.py`

**Step 1: 添加生成方法**

在 `StaticSiteGenerator` 类中添加以下方法：

```python
def _generate_moments(self):
    """生成说说页"""
    from ..models import Moment
    
    print("[MOMENTS] 生成说说页...")
    moments = Moment.objects.all().order_by("-created_at")
    
    context = self._get_context(
        {"moments": moments},
        static_prefix="static",
        url_prefix="",
    )
    html = render_to_string("pages/moments.html", context)
    self._write_page("moments.html", html)


def _generate_friends(self):
    """生成友链页"""
    from ..models import FriendLink
    
    print("[FRIENDS] 生成友链页...")
    links = FriendLink.objects.filter(is_active=True).order_by("order", "name")
    
    context = self._get_context(
        {"links": links},
        static_prefix="static",
        url_prefix="",
    )
    html = render_to_string("pages/friends.html", context)
    self._write_page("friends.html", html)


def _generate_about(self):
    """生成关于页"""
    from ..models import SiteConfig
    
    print("[ABOUT] 生成关于页...")
    about_content = SiteConfig.get("about.content", "")
    
    context = self._get_context(
        {"about_content": about_content},
        static_prefix="static",
        url_prefix="",
    )
    html = render_to_string("pages/about.html", context)
    self._write_page("about.html", html)
```

**Step 2: 更新 generate_all 方法**

在 `generate_all` 方法中添加调用：

```python
def generate_all(self):
    """生成全部静态页面"""
    print("[START] 开始生成静态站点...")
    
    self._clean_output()
    self._copy_static()
    
    self._generate_index()
    self._generate_posts()
    self._generate_archive()
    self._generate_tags()
    self._generate_categories()
    self._generate_moments()    # 新增
    self._generate_friends()    # 新增
    self._generate_about()      # 新增
    
    print("[DONE] 静态站点生成完成！")
```

**Step 3: Commit**

```bash
git add blog/utils/generator.py
git commit -m "feat: add moments, friends, about page generation"
```

---

## Phase 3.3: push 命令实现

### Task 5: 创建 push 管理命令

**Files:**
- Create: `blog/management/commands/push.py`

**Step 1: 创建 push.py**

```python
"""
推送静态站点到 GitHub Pages
用法: python manage.py push
"""
import subprocess
from django.core.management.base import BaseCommand
from django.conf import settings
from pathlib import Path


class Command(BaseCommand):
    help = "推送静态站点到 GitHub Pages"
    
    def add_arguments(self, parser):
        parser.add_argument(
            "--message", "-m",
            default="Update blog content",
            help="提交消息",
        )
        parser.add_argument(
            "--no-push",
            action="store_true",
            help="只提交，不推送",
        )
    
    def handle(self, *args, **options):
        output_dir = Path(settings.OUTPUT_DIR)
        
        if not (output_dir / ".git").exists():
            self.stdout.write(self.style.ERROR(
                "❌ output 目录不是 git 仓库，请先运行: python manage.py init_output"
            ))
            return
        
        try:
            # 检查是否有变更
            result = subprocess.run(
                ["git", "status", "--porcelain"],
                cwd=output_dir,
                capture_output=True,
                text=True,
            )
            
            if not result.stdout.strip():
                self.stdout.write(self.style.WARNING("没有变更需要提交"))
                return
            
            # 添加所有变更
            self.stdout.write("📦 添加变更...")
            subprocess.run(["git", "add", "."], cwd=output_dir, check=True)
            
            # 提交
            message = options["message"]
            self.stdout.write(f"💾 提交: {message}")
            subprocess.run(
                ["git", "commit", "-m", message],
                cwd=output_dir,
                check=True,
            )
            
            if options["no_push"]:
                self.stdout.write(self.style.SUCCESS("✅ 已提交，未推送"))
                return
            
            # 推送（带重试）
            for attempt in range(3):
                try:
                    self.stdout.write(f"🚀 推送中... (尝试 {attempt + 1}/3)")
                    subprocess.run(
                        ["git", "push"],
                        cwd=output_dir,
                        check=True,
                    )
                    self.stdout.write(self.style.SUCCESS("✅ 推送成功！"))
                    return
                except subprocess.CalledProcessError as e:
                    if attempt < 2:
                        self.stdout.write(self.style.WARNING(
                            f"推送失败，正在重试..."
                        ))
                    else:
                        raise
            
        except subprocess.CalledProcessError as e:
            self.stdout.write(self.style.ERROR(f"❌ 操作失败: {e}"))
            self.stdout.write("你可以稍后手动执行:")
            self.stdout.write("  cd output")
            self.stdout.write("  git add .")
            self.stdout.write(f'  git commit -m "{options["message"]}"')
            self.stdout.write("  git push")
```

**Step 2: Commit**

```bash
git add blog/management/commands/push.py
git commit -m "feat: add push command for GitHub Pages deployment"
```

---

## Phase 3.4: 最终验证

### Task 6: 验证所有功能

**Step 1: 检查模板文件**

Run: `ls -la site_templates/pages/`

Expected: 应该包含 9 个文件：
- index.html ✅
- post.html ✅
- archive.html ✅
- tags.html ✅
- tag.html ✅
- category.html ✅
- moments.html ✅ (新增)
- friends.html ✅ (新增)
- about.html ✅ (新增)

**Step 2: 生成静态页面**

Run: `python manage.py generate`

Expected output:
```
[START] 开始生成静态站点...
[CLEAN] 清理输出目录...
[STATIC] 复制静态资源...
[PAGE] 生成首页...
[POSTS] 生成博文页面...
[ARCHIVE] 生成归档页...
[TAGS] 生成标签页...
[CATEGORIES] 生成分类页...
[MOMENTS] 生成说说页...
[FRIENDS] 生成友链页...
[ABOUT] 生成关于页...
[DONE] 静态站点生成完成！
```

**Step 3: 检查输出目录**

Run: `ls -la output/`

Expected:
```
index.html
archive.html
moments.html
friends.html
about.html
posts/
tags/
categories/
static/
```

**Step 4: 验证 push 命令**

Run: `python manage.py push --help`

Expected: 显示命令帮助信息

**Step 5: 最终 Commit**

```bash
git add .
git commit -m "feat: complete Phase 3 - full template system and push command"
```

---

## Phase 3 完成标志

- [ ] moments.html 页面正常生成
- [ ] friends.html 页面正常生成
- [ ] about.html 页面正常生成
- [ ] `python manage.py generate` 生成所有 8 种页面
- [ ] `python manage.py push` 命令可用
- [ ] 静态站点可部署到 GitHub Pages

---

## 使用说明

### 生成静态页面

```bash
python manage.py generate
```

### 推送到 GitHub Pages

```bash
# 首次使用（如果 output 还不是 git 仓库）
python manage.py init_output
cd output
git remote add origin https://github.com/username/username.github.io.git
git branch -M main
git push -u origin main
cd ..

# 日常使用
python manage.py push
```

### 配置站点信息

在 Admin 的"站点配置"中添加：

| Key | Value | 说明 |
|-----|-------|------|
| site.title | 我的博客 | 站点标题 |
| site.description | 一个技术博客 | 站点描述 |
| author.name | 张三 | 作者名称 |
| site.url | https://example.com | 站点 URL |
| about.content | # 关于我\n... | 关于页面内容 |

---

## 后续扩展 (Phase 4+)

- [ ] 全文搜索 (SQLite FTS5)
- [ ] 评论系统 (Giscus / Waline)
- [ ] RSS 订阅
- [ ] 主题切换
- [ ] 国际化