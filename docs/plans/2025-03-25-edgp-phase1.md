# EDGP Phase 1: 骨架搭建 - 实现计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 创建可运行的 Django 博客骨架，通过 Admin 管理 Post/Category/Tag

**Architecture:** Django 5.x + SQLite + django-simpleui + Bootstrap 5 本地化。数据模型支持博文、分类、标签。Admin 集成 EasyMDE Markdown 编辑器。

**Tech Stack:** Django 5.x, SQLite, django-simpleui, Bootstrap 5, markdown-it-py, pygments

---

## Task 1: 创建 Django 项目结构

**Files:**
- Create: `manage.py`
- Create: `edgp/__init__.py`
- Create: `edgp/settings.py`
- Create: `edgp/urls.py`
- Create: `edgp/wsgi.py`
- Create: `requirements.txt`

**Step 1: 创建项目目录结构**

```bash
mkdir -p edgp blog/blog site_templates/components site_templates/pages static/vendor/easymde static/vendor/bootstrap media/uploads output
```

**Step 2: 创建 requirements.txt**

```txt
# requirements.txt
Django>=5.0
django-simpleui>=2024.1
Pillow>=10.0
markdown-it-py>=3.0
mdit-py-plugins>=0.4
pygments>=2.17
pyyaml>=6.0
python-slugify>=8.0
```

**Step 3: 创建 manage.py**

```python
#!/usr/bin/env python
"""Django's command-line utility for administrative tasks."""
import os
import sys


def main():
    """Run administrative tasks."""
    os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'edgp.settings')
    try:
        from django.core.management import execute_from_command_line
    except ImportError as exc:
        raise ImportError(
            "Couldn't import Django. Are you sure it's installed and "
            "available on your PYTHONPATH environment variable? Did you "
            "forget to activate a virtual environment?"
        ) from exc
    execute_from_command_line(sys.argv)


if __name__ == '__main__':
    main()
```

**Step 4: 创建 edgp/settings.py**

```python
"""
Django settings for edgp project.
"""
from pathlib import Path

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'django-insecure-change-this-in-production-key-here'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = ['*']

# Application definition
INSTALLED_APPS = [
    'simpleui',
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'blog',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'edgp.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [BASE_DIR / 'site_templates'],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'edgp.wsgi.application'

# Database
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}

# Password validation
AUTH_PASSWORD_VALIDATORS = [
    {'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator'},
    {'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator'},
    {'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator'},
    {'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator'},
]

# Internationalization
LANGUAGE_CODE = 'zh-hans'
TIME_ZONE = 'Asia/Shanghai'
USE_I18N = True
USE_TZ = True

# Static files (CSS, JavaScript, Images)
STATIC_URL = '/static/'
STATICFILES_DIRS = [BASE_DIR / 'static']
STATIC_ROOT = BASE_DIR / 'staticfiles'

# Media files
MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'

# Default primary key field type
DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

# Custom settings for EDGP
OUTPUT_DIR = BASE_DIR / 'output'
SITE_TEMPLATES_DIR = BASE_DIR / 'site_templates'
STATIC_SRC_DIR = BASE_DIR / 'static'

# SimpleUI settings
SIMPLEUI_HOME_TITLE = 'EDGP 博客管理'
SIMPLEUI_INDEX = 'blog:admin_dashboard'
SIMPLEUI_DEFAULT_THEME = 'admin.lte.css'
SIMPLEUI_LOGO = 'https://img.icons8.com/fluency/48/blog.png'
```

**Step 5: 创建 edgp/urls.py**

```python
"""
URL configuration for edgp project.
"""
from django.contrib import admin
from django.urls import path, include
from django.conf import settings
from django.conf.urls.static import static

urlpatterns = [
    path('admin/', admin.site.urls),
]

if settings.DEBUG:
    urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)
```

**Step 6: 创建 edgp/wsgi.py**

```python
"""
WSGI config for edgp project.
"""
import os
from django.core.wsgi import get_wsgi_application
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'edgp.settings')
application = get_wsgi_application()
```

**Step 7: 创建 edgp/__init__.py**

```python
"""
EDGP - 静态博客管理系统
"""
```

**Step 8: 验证项目创建成功**

Run: `python manage.py check`
Expected: "System check identified no issues (0 silenced)."

**Step 9: Commit**

```bash
git add manage.py edgp/ requirements.txt
git commit -m "feat: init Django project structure"
```

---

## Task 2: 创建 blog 应用基础结构

**Files:**
- Create: `blog/__init__.py`
- Create: `blog/apps.py`
- Create: `blog/urls.py`

**Step 1: 创建 blog/__init__.py**

```python
"""
Blog application for EDGP.
"""
```

**Step 2: 创建 blog/apps.py**

```python
from django.apps import AppConfig


class BlogConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'blog'
    verbose_name = '博客管理'
```

**Step 3: 创建 blog/urls.py**

```python
from django.urls import path
from . import views

app_name = 'blog'

urlpatterns = [
    # 后续添加路由
]
```

**Step 4: 验证应用创建**

Run: `python manage.py check`
Expected: "System check identified no issues (0 silenced)."

**Step 5: Commit**

```bash
git add blog/
git commit -m "feat: create blog app structure"
```

---

## Task 3: 创建 Category 数据模型

**Files:**
- Modify: `blog/models.py`

**Step 1: 创建 blog/models.py**

```python
from django.db import models
from django.utils.text import slugify


class Category(models.Model):
    """分类"""
    name = models.CharField('分类名称', max_length=50)
    slug = models.SlugField('URL标识', unique=True, blank=True)
    description = models.TextField('分类描述', blank=True)
    order = models.IntegerField('排序', default=0)
    
    class Meta:
        verbose_name = '分类'
        verbose_name_plural = '分类'
        ordering = ['order', 'name']
    
    def __str__(self):
        return self.name
    
    def save(self, *args, **kwargs):
        if not self.slug:
            self.slug = slugify(self.name)
        super().save(*args, **kwargs)
    
    @property
    def post_count(self):
        return self.posts.count()
```

**Step 2: 执行数据库迁移**

Run: `python manage.py makemigrations blog`
Expected: "Create model Category"

Run: `python manage.py migrate`
Expected: "Running migrations: ... Applying blog.0001_initial... OK"

**Step 3: Commit**

```bash
git add blog/models.py blog/migrations/
git commit -m "feat: add Category model"
```

---

## Task 4: 创建 Tag 数据模型

**Files:**
- Modify: `blog/models.py`

**Step 1: 添加 Tag 模型到 blog/models.py**

```python
class Tag(models.Model):
    """标签"""
    name = models.CharField('标签名称', max_length=50)
    slug = models.SlugField('URL标识', unique=True, blank=True)
    
    class Meta:
        verbose_name = '标签'
        verbose_name_plural = '标签'
        ordering = ['name']
    
    def __str__(self):
        return self.name
    
    def save(self, *args, **kwargs):
        if not self.slug:
            self.slug = slugify(self.name)
        super().save(*args, **kwargs)
    
    @property
    def post_count(self):
        return self.posts.count()
```

**Step 2: 执行数据库迁移**

Run: `python manage.py makemigrations blog`
Run: `python manage.py migrate`

**Step 3: Commit**

```bash
git add blog/models.py blog/migrations/
git commit -m "feat: add Tag model"
```

---

## Task 5: 创建 Post 数据模型

**Files:**
- Modify: `blog/models.py`

**Step 1: 添加 Post 模型到 blog/models.py**

```python
class Post(models.Model):
    """博文"""
    STATUS_CHOICES = [
        ('draft', '草稿'),
        ('published', '已发布'),
    ]
    
    title = models.CharField('标题', max_length=200)
    slug = models.SlugField('URL标识', unique=True, blank=True)
    content = models.TextField('内容 (Markdown)')
    content_html = models.TextField('渲染后HTML', blank=True)
    status = models.CharField('状态', max_length=20, choices=STATUS_CHOICES, default='draft')
    
    category = models.ForeignKey(
        Category, 
        on_delete=models.SET_NULL, 
        null=True, 
        blank=True, 
        verbose_name='分类',
        related_name='posts'
    )
    tags = models.ManyToManyField(Tag, blank=True, verbose_name='标签', related_name='posts')
    
    created_at = models.DateTimeField('创建时间', auto_now_add=True)
    updated_at = models.DateTimeField('更新时间', auto_now=True)
    published_at = models.DateTimeField('发布时间', null=True, blank=True)
    cover_image = models.ImageField('封面图', upload_to='covers/', blank=True)
    
    class Meta:
        verbose_name = '博文'
        verbose_name_plural = '博文'
        ordering = ['-published_at', '-created_at']
    
    def __str__(self):
        return self.title
    
    def save(self, *args, **kwargs):
        if not self.slug:
            self.slug = slugify(self.title)
        # TODO: 在 Phase 2 添加 Markdown 渲染
        super().save(*args, **kwargs)
    
    def tag_list(self):
        return ', '.join([tag.name for tag in self.tags.all()])
    tag_list.short_description = '标签'
```

**Step 2: 执行数据库迁移**

Run: `python manage.py makemigrations blog`
Run: `python manage.py migrate`

**Step 3: Commit**

```bash
git add blog/models.py blog/migrations/
git commit -m "feat: add Post model with Category and Tag relations"
```

---

## Task 6: 配置 django-simpleui Admin

**Files:**
- Modify: `edgp/settings.py`
- Create: `blog/admin.py`

**Step 1: 更新 edgp/settings.py 添加 simpleui 配置**

在文件末尾添加：

```python
# SimpleUI 配置
SIMPLEUI_HOME_TITLE = 'EDGP 博客管理'
SIMPLEUI_HOME_INDEX = '/admin/'
SIMPLEUI_DEFAULT_THEME = 'admin.lte.css'
SIMPLEUI_LOGO = 'https://img.icons8.com/fluency/48/blog.png'

# 隐藏首页服务器信息
SIMPLEUI_HOME_INFO = False
SIMPLEUI_HOME_QUICK = True
SIMPLEUI_HOME_ACTION = True
```

**Step 2: 创建 blog/admin.py**

```python
from django.contrib import admin
from .models import Post, Category, Tag


@admin.register(Category)
class CategoryAdmin(admin.ModelAdmin):
    list_display = ['name', 'slug', 'order', 'post_count']
    list_editable = ['order']
    prepopulated_fields = {'slug': ('name',)}
    search_fields = ['name']
    
    def post_count(self, obj):
        return obj.post_count
    post_count.short_description = '文章数'


@admin.register(Tag)
class TagAdmin(admin.ModelAdmin):
    list_display = ['name', 'slug', 'post_count']
    prepopulated_fields = {'slug': ('name',)}
    search_fields = ['name']
    
    def post_count(self, obj):
        return obj.post_count
    post_count.short_description = '文章数'


@admin.register(Post)
class PostAdmin(admin.ModelAdmin):
    list_display = ['title', 'category', 'status', 'published_at', 'tag_list']
    list_filter = ['status', 'category', 'tags', 'created_at']
    search_fields = ['title', 'content']
    filter_horizontal = ['tags']
    date_hierarchy = 'published_at'
    prepopulated_fields = {'slug': ('title',)}
    
    fieldsets = (
        ('基本信息', {
            'fields': ('title', 'slug', 'status', 'category', 'tags')
        }),
        ('内容', {
            'fields': ('content', 'cover_image')
        }),
        ('时间', {
            'fields': ('published_at',),
            'classes': ('collapse',)
        }),
    )
```

**Step 3: 创建超级用户**

Run: `python manage.py createsuperuser --username admin --email admin@example.com`
Input password when prompted.

**Step 4: 验证 Admin**

Run: `python manage.py runserver`
Visit: http://127.0.0.1:8000/admin/
Login with admin credentials.
Expected: See simpleui dashboard with 博文, 分类, 标签 models.

**Step 5: Commit**

```bash
git add edgp/settings.py blog/admin.py
git commit -m "feat: configure django-simpleui admin"
```

---

## Task 7: 下载 Bootstrap 5 本地资源

**Files:**
- Create: `static/vendor/bootstrap/css/bootstrap.min.css`
- Create: `static/vendor/bootstrap/js/bootstrap.bundle.min.js`

**Step 1: 创建目录结构**

```bash
mkdir -p static/vendor/bootstrap/css static/vendor/bootstrap/js
```

**Step 2: 下载 Bootstrap CSS**

Run from project root:
```bash
curl -L -o static/vendor/bootstrap/css/bootstrap.min.css https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css
```

**Step 3: 下载 Bootstrap JS**

Run from project root:
```bash
curl -L -o static/vendor/bootstrap/js/bootstrap.bundle.min.js https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js
```

**Step 4: 验证文件存在**

Run: `ls -la static/vendor/bootstrap/css/` and `ls -la static/vendor/bootstrap/js/`
Expected: Files exist with non-zero size.

**Step 5: Commit**

```bash
git add static/vendor/bootstrap/
git commit -m "feat: add Bootstrap 5 local assets"
```

---

## Task 8: 下载 EasyMDE 编辑器资源

**Files:**
- Create: `static/vendor/easymde/easymde.min.css`
- Create: `static/vendor/easymde/easymde.min.js`

**Step 1: 创建目录结构**

```bash
mkdir -p static/vendor/easymde
```

**Step 2: 下载 EasyMDE CSS**

```bash
curl -L -o static/vendor/easymde/easymde.min.css https://cdn.jsdelivr.net/npm/easymde/dist/easymde.min.css
```

**Step 3: 下载 EasyMDE JS**

```bash
curl -L -o static/vendor/easymde/easymde.min.js https://cdn.jsdelivr.net/npm/easymde/dist/easymde.min.js
```

**Step 4: 验证文件存在**

Run: `ls -la static/vendor/easymde/`
Expected: Both CSS and JS files exist.

**Step 5: Commit**

```bash
git add static/vendor/easymde/
git commit -m "feat: add EasyMDE editor local assets"
```

---

## Task 9: 创建 base.html 基础模板

**Files:**
- Create: `site_templates/base.html`

**Step 1: 创建 site_templates/base.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{% block meta_description %}{{ site.description|default:'' }}{% endblock %}">
    {% include 'components/header.html' %}
    <title>{% block title %}{{ site.title|default:'我的博客' }}{% endblock %}</title>
    <link rel="stylesheet" href="/static/vendor/bootstrap/css/bootstrap.min.css">
    {% block extra_head %}{% endblock %}
</head>
<body>
    {% include 'components/navbar.html' %}
    
    <main class="container my-4">
        {% block content %}{% endblock %}
    </main>
    
    {% include 'components/footer.html' %}
    
    <script src="/static/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
    {% block extra_scripts %}{% endblock %}
</body>
</html>
```

**Step 2: Commit**

```bash
git add site_templates/base.html
git commit -m "feat: add base.html template"
```

---

## Task 10: 创建 navbar 和 footer 组件

**Files:**
- Create: `site_templates/components/navbar.html`
- Create: `site_templates/components/footer.html`
- Create: `site_templates/components/header.html`

**Step 1: 创建 site_templates/components/header.html**

```html
<!-- Meta headers for SEO -->
<meta name="author" content="{{ site.author|default:'' }}">
<meta name="generator" content="EDGP - Django Static Blog Generator">
```

**Step 2: 创建 site_templates/components/navbar.html**

```html
<nav class="navbar navbar-expand-lg navbar-dark bg-primary sticky-top">
    <div class="container">
        <a class="navbar-brand" href="/">{{ site.title|default:'我的博客' }}</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/">首页</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/archive.html">归档</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/tags/">标签</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/moments.html">说说</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/friends.html">友链</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/about.html">关于</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
```

**Step 3: 创建 site_templates/components/footer.html**

```html
<footer class="bg-dark text-light py-4 mt-5">
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <p class="mb-0">&copy; {{ site.title|default:'我的博客' }}</p>
            </div>
            <div class="col-md-6 text-md-end">
                <p class="mb-0">Powered by <a href="https://github.com" class="text-light">EDGP</a></p>
            </div>
        </div>
    </div>
</footer>
```

**Step 4: Commit**

```bash
git add site_templates/components/
git commit -m "feat: add navbar, footer, header components"
```

---

## Task 11: 创建首页模板

**Files:**
- Create: `site_templates/pages/index.html`

**Step 1: 创建 site_templates/pages/index.html**

```html
{% extends 'base.html' %}

{% block title %}{{ site.title|default:'我的博客' }}{% endblock %}

{% block content %}
<div class="row">
    <div class="col-lg-8">
        <h1 class="mb-4">最新文章</h1>
        
        {% for post in posts %}
        <article class="card mb-4">
            {% if post.cover_image %}
            <img src="{{ post.cover_image.url }}" class="card-img-top" alt="{{ post.title }}">
            {% endif %}
            <div class="card-body">
                <h2 class="card-title">
                    <a href="/posts/{{ post.published_at.year }}/{{ post.slug }}.html" class="text-decoration-none">
                        {{ post.title }}
                    </a>
                </h2>
                <p class="card-text text-muted">
                    <small>
                        {{ post.published_at|date:"Y-m-d" }}
                        {% if post.category %}
                        · <a href="/categories/{{ post.category.slug }}.html">{{ post.category.name }}</a>
                        {% endif %}
                    </small>
                </p>
                <p class="card-text">{{ post.content|truncatewords:50|striptags }}...</p>
                <a href="/posts/{{ post.published_at.year }}/{{ post.slug }}.html" class="btn btn-primary">阅读全文</a>
            </div>
        </article>
        {% empty %}
        <div class="alert alert-info">暂无文章</div>
        {% endfor %}
    </div>
    
    <div class="col-lg-4">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">分类</h5>
            </div>
            <div class="card-body">
                <ul class="list-unstyled mb-0">
                    {% for category in categories %}
                    <li><a href="/categories/{{ category.slug }}.html">{{ category.name }}</a> ({{ category.post_count }})</li>
                    {% empty %}
                    <li class="text-muted">暂无分类</li>
                    {% endfor %}
                </ul>
            </div>
        </div>
        
        <div class="card mt-3">
            <div class="card-header">
                <h5 class="mb-0">标签</h5>
            </div>
            <div class="card-body">
                {% for tag in tags %}
                <a href="/tags/{{ tag.slug }}.html" class="badge bg-secondary text-decoration-none me-1">{{ tag.name }}</a>
                {% empty %}
                <span class="text-muted">暂无标签</span>
                {% endfor %}
            </div>
        </div>
    </div>
</div>
{% endblock %}
```

**Step 2: Commit**

```bash
git add site_templates/pages/index.html
git commit -m "feat: add index.html homepage template"
```

---

## Task 12: 创建文章详情模板

**Files:**
- Create: `site_templates/pages/post.html`
- Create: `site_templates/components/prev_next.html`

**Step 1: 创建 site_templates/components/prev_next.html**

```html
<nav class="post-navigation border-top border-bottom py-3 my-4">
    <div class="row">
        <div class="col-6">
            {% if prev_post %}
            <a href="/posts/{{ prev_post.published_at.year }}/{{ prev_post.slug }}.html" class="text-decoration-none">
                <span class="text-muted">← 上一篇</span><br>
                <span>{{ prev_post.title|truncatechars:30 }}</span>
            </a>
            {% endif %}
        </div>
        <div class="col-6 text-end">
            {% if next_post %}
            <a href="/posts/{{ next_post.published_at.year }}/{{ next_post.slug }}.html" class="text-decoration-none">
                <span class="text-muted">下一篇 →</span><br>
                <span>{{ next_post.title|truncatechars:30 }}</span>
            </a>
            {% endif %}
        </div>
    </div>
</nav>
```

**Step 2: 创建 site_templates/pages/post.html**

```html
{% extends 'base.html' %}

{% block title %}{{ post.title }} - {{ site.title|default:'我的博客' }}{% endblock %}

{% block content %}
<article class="post">
    <header class="post-header mb-4">
        <h1 class="display-5">{{ post.title }}</h1>
        <div class="text-muted">
            <time datetime="{{ post.published_at|date:'c' }}">
                {{ post.published_at|date:"Y年m月d日" }}
            </time>
            {% if post.category %}
            <span class="ms-2">
                <a href="/categories/{{ post.category.slug }}.html" class="text-decoration-none">
                    {{ post.category.name }}
                </a>
            </span>
            {% endif %}
            {% for tag in post.tags.all %}
            <span class="ms-2">
                <a href="/tags/{{ tag.slug }}.html" class="badge bg-secondary text-decoration-none">
                    #{{ tag.name }}
                </a>
            </span>
            {% endfor %}
        </div>
    </header>
    
    {% if post.cover_image %}
    <div class="post-cover mb-4">
        <img src="{{ post.cover_image.url }}" alt="{{ post.title }}" class="img-fluid rounded">
    </div>
    {% endif %}
    
    <div class="post-content">
        {{ post.content_html|safe|default:post.content|linebreaks }}
    </div>
    
    {% include 'components/prev_next.html' %}
</article>
{% endblock %}

{% block extra_scripts %}
{# Phase 2: mermaid, katex 脚本 #}
{% endblock %}
```

**Step 3: Commit**

```bash
git add site_templates/pages/post.html site_templates/components/prev_next.html
git commit -m "feat: add post.html detail template with navigation"
```

---

## Task 13: 创建 Markdown 渲染工具函数

**Files:**
- Create: `blog/utils/__init__.py`
- Create: `blog/utils/markdown.py`

**Step 1: 创建 blog/utils/__init__.py**

```python
"""
Blog utility functions.
"""
from .markdown import render_markdown

__all__ = ['render_markdown']
```

**Step 2: 创建 blog/utils/markdown.py**

```python
"""
Markdown 渲染工具
Phase 1: 基础渲染
Phase 2: 添加 mermaid, LaTeX, 代码高亮支持
"""
from markdown_it import MarkdownIt
from pygments import highlight
from pygments.lexers import get_lexer_by_name, TextLexer
from pygments.formatters import HtmlFormatter


def render_markdown(text: str) -> str:
    """
    渲染 Markdown 文本为 HTML
    
    Args:
        text: Markdown 文本
        
    Returns:
        渲染后的 HTML 字符串
    """
    if not text:
        return ''
    
    # Phase 1: 基础渲染
    md = MarkdownIt('commonmark', {'html': True, 'linkify': True})
    md.enable('table')
    md.enable('strikethrough')
    
    # 自定义代码块处理
    def code_block_renderer(tokens, idx, options, env):
        token = tokens[idx]
        info = token.info.strip() if token.info else ''
        code = token.content
        
        if info:
            try:
                lexer = get_lexer_by_name(info, stripall=True)
            except Exception:
                lexer = TextLexer()
        else:
            lexer = TextLexer()
        
        formatter = HtmlFormatter(cssclass='highlight', linenos=False)
        return highlight(code, lexer, formatter)
    
    # 注册自定义渲染器
    md.add_render_rule('fence', code_block_renderer)
    
    return md.render(text)
```

**Step 3: Commit**

```bash
git add blog/utils/
git commit -m "feat: add markdown renderer with pygments support"
```

---

## Task 14: 集成 EasyMDE 到 Admin

**Files:**
- Modify: `blog/admin.py`
- Create: `static/js/admin-post.js`

**Step 1: 创建 static/js/admin-post.js**

```javascript
// EasyMDE 编辑器初始化
document.addEventListener('DOMContentLoaded', function() {
    const contentTextarea = document.getElementById('id_content');
    if (contentTextarea && typeof EasyMDE !== 'undefined') {
        const easyMDE = new EasyMDE({
            element: contentTextarea,
            spellChecker: false,
            autosave: {
                enabled: true,
                uniqueId: 'edgp-post-content',
                delay: 5000,
            },
            placeholder: '在此输入 Markdown 内容...',
            status: ['autosave', 'lines', 'words'],
            toolbar: [
                'bold', 'italic', 'heading', '|',
                'quote', 'unordered-list', 'ordered-list', '|',
                'link', 'image', 'code', '|',
                'preview', 'side-by-side', 'fullscreen', '|',
                'guide'
            ],
            previewRender: function(plainText) {
                return plainText; // Phase 2: 使用后端渲染 API
            }
        });
    }
});
```

**Step 2: 更新 blog/admin.py 添加 Media 类**

```python
@admin.register(Post)
class PostAdmin(admin.ModelAdmin):
    # ... 现有代码 ...
    
    class Media:
        css = {
            'all': ('/static/vendor/easymde/easymde.min.css',)
        }
        js = (
            '/static/vendor/easymde/easymde.min.js',
            '/static/js/admin-post.js',
        )
```

**Step 3: Commit**

```bash
git add static/js/admin-post.js blog/admin.py
git commit -m "feat: integrate EasyMDE markdown editor into Post admin"
```

---

## Task 15: 创建初始化 output 目录脚本

**Files:**
- Create: `blog/management/__init__.py`
- Create: `blog/management/commands/__init__.py`
- Create: `blog/management/commands/init_output.py`

**Step 1: 创建目录结构**

```bash
mkdir -p blog/management/commands
```

**Step 2: 创建 blog/management/__init__.py**

```python
"""
Blog management commands.
"""
```

**Step 3: 创建 blog/management/commands/__init__.py**

```python
"""
Management commands for blog.
"""
```

**Step 4: 创建 blog/management/commands/init_output.py**

```python
"""
初始化 output 目录为独立的 git 仓库
用于 GitHub Pages 部署
"""
from django.core.management.base import BaseCommand
from django.conf import settings
from pathlib import Path
import subprocess


class Command(BaseCommand):
    help = '初始化 output 目录为独立的 git 仓库'
    
    def handle(self, *args, **options):
        output_dir = Path(settings.OUTPUT_DIR)
        
        if not output_dir.exists():
            output_dir.mkdir(parents=True)
            self.stdout.write(f'创建目录: {output_dir}')
        
        git_dir = output_dir / '.git'
        
        if git_dir.exists():
            self.stdout.write(self.style.WARNING(f'output 目录已初始化: {output_dir}'))
            return
        
        # 初始化 git 仓库
        try:
            subprocess.run(['git', 'init'], cwd=output_dir, check=True)
            self.stdout.write(self.style.SUCCESS(f'✅ Git 仓库初始化成功: {output_dir}'))
            
            # 创建 .gitkeep
            (output_dir / '.gitkeep').touch()
            
            # 提示后续操作
            self.stdout.write('\n后续步骤:')
            self.stdout.write('  1. cd output')
            self.stdout.write('  2. git remote add origin <your-github-pages-repo-url>')
            self.stdout.write('  3. git branch -M main')
            self.stdout.write('  4. git push -u origin main')
            
        except subprocess.CalledProcessError as e:
            self.stdout.write(self.style.ERROR(f'❌ 初始化失败: {e}'))
```

**Step 5: 验证命令**

Run: `python manage.py init_output`
Expected: "✅ Git 仓库初始化成功"

Run: `ls -la output/.git`
Expected: .git directory exists.

**Step 6: Commit**

```bash
git add blog/management/
git commit -m "feat: add init_output management command"
```

---

## Task 16: 验证 Phase 1 完成

**Step 1: 运行全部检查**

Run: `python manage.py check`
Expected: "System check identified no issues"

**Step 2: 运行开发服务器**

Run: `python manage.py runserver`

**Step 3: 访问 Admin**

Visit: http://127.0.0.1:8000/admin/
Login: admin / <your-password>

**Step 4: 创建测试数据**

在 Admin 中创建：
1. 创建一个分类 "技术"
2. 创建标签 "Python", "Django"
3. 创建一篇博文，选择分类和标签，状态设为"已发布"

**Step 5: 验证 EasyMDE 编辑器**

在创建/编辑博文页面，确认 Markdown 编辑器正常显示。

**Step 6: 确认数据库**

Run: `python manage.py dbshell`
Run SQL: `SELECT * FROM blog_post;`
Expected: 看到刚创建的文章数据

**Step 7: 最终 Commit**

```bash
git add .
git commit -m "feat: complete Phase 1 - MVP skeleton ready"
```

---

## Phase 1 完成标志

- [x] Django 项目结构完整
- [x] 数据模型 (Post, Category, Tag) 创建并可迁移
- [x] django-simpleui Admin 正常运行
- [x] EasyMDE Markdown 编辑器集成
- [x] Bootstrap 5 本地化资源
- [x] 基础模板系统 (base.html, navbar, footer, index, post)
- [x] output 目录初始化为独立 git 仓库
- [x] 可通过 Admin 创建和管理博文

---

## Phase 2 预览

下一阶段将实现：
1. 完整的 Markdown 渲染器 (mermaid, LaTeX)
2. 静态页面生成器 (StaticSiteGenerator)
3. Moment, FriendLink, SiteConfig 模型
4. `python manage.py generate` 命令

---

## 执行选项

**Plan complete and saved to `docs/plans/2025-03-25-edgp-phase1.md`.**

两种执行方式：

**1. Subagent-Driven (当前会话)** - 我逐任务派发子代理，每个任务完成后审查

**2. Parallel Session (新会话)** - 在新会话中使用 executing-plans skill 批量执行

选择哪种方式？