# EDGP Phase 2: 核心功能 - 执行计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 实现静态页面生成器 + 完整 Markdown 渲染 + 完整数据模型

**Architecture:** 
- Markdown 渲染器：markdown-it-py + mdit-py-plugins + pygments
- 静态生成器：Django 模板渲染 → 输出到 output/
- 数据模型：扩展 Moment, FriendLink, SiteConfig

**Tech Stack:** markdown-it-py, mdit-py-plugins, pygments, KaTeX (前端), Mermaid (前端)

---

## Phase 2 任务概览

```
Phase 2.1: Markdown 渲染器增强 (4 tasks)
Phase 2.2: 静态页面生成器 (6 tasks)
Phase 2.3: 完整数据模型 (3 tasks)
Phase 2.4: Management Commands (2 tasks)
```

---

## Phase 2.1: Markdown 渲染器增强

### Task 1: 增强 markdown.py 支持 mermaid 和 LaTeX

**Files:**
- Modify: `blog/utils/markdown.py`

**Step 1: 更新 markdown.py**

```python
"""
Markdown 渲染工具
支持：基础语法、mermaid 图表、LaTeX 公式、代码高亮
"""
from markdown_it import MarkdownIt
from mdit_py_plugins.front_matter import front_matter_plugin
from mdit_py_plugins.footnote import footnote_plugin
from pygments import highlight
from pygments.lexers import get_lexer_by_name, TextLexer
from pygments.formatters import HtmlFormatter
import re


class MarkdownRenderer:
    """Markdown 渲染器"""
    
    def __init__(self):
        self.md = MarkdownIt('commonmark', {'html': True, 'linkify': True})
        self.md.enable('table')
        self.md.enable('strikethrough')
        self.md.enable('tasklists')
        self.md.use(front_matter_plugin)
        self.md.use(footnote_plugin)
        
        # 自定义 fence 渲染
        self.md.add_render_rule('fence', self._render_fence)
    
    def _render_fence(self, tokens, idx, options, env):
        """自定义代码块渲染"""
        token = tokens[idx]
        info = token.info.strip().lower() if token.info else ''
        code = token.content
        
        # mermaid 图表
        if info == 'mermaid':
            return f'<div class="mermaid">{code}</div>\n'
        
        # LaTeX 公式块
        if info in ['math', 'latex', 'katex']:
            return f'<div class="katex-display">{code}</div>\n'
        
        # 代码高亮
        if info:
            try:
                lexer = get_lexer_by_name(info, stripall=True)
            except Exception:
                lexer = TextLexer()
        else:
            lexer = TextLexer()
        
        formatter = HtmlFormatter(
            cssclass='highlight',
            linenos=False,
            nowrap=False
        )
        return highlight(code, lexer, formatter)
    
    def render(self, text: str) -> str:
        """渲染 Markdown 文本"""
        if not text:
            return ''
        return self.md.render(text)


# 全局渲染器实例
_renderer = MarkdownRenderer()


def render_markdown(text: str) -> str:
    """
    渲染 Markdown 文本为 HTML
    
    Args:
        text: Markdown 文本
        
    Returns:
        渲染后的 HTML 字符串
    """
    return _renderer.render(text)
```

**Step 2: 更新 blog/utils/__init__.py**

```python
"""
Blog utility functions.
"""
from .markdown import render_markdown, MarkdownRenderer

__all__ = ['render_markdown', 'MarkdownRenderer']
```

**Step 3: Commit**

```bash
git add blog/utils/
git commit -m "feat: enhance markdown renderer with mermaid and latex support"
```

---

### Task 2: 下载 KaTeX 和 Mermaid 前端资源

**Files:**
- Create: `static/vendor/katex/katex.min.css`
- Create: `static/vendor/katex/katex.min.js`
- Create: `static/vendor/mermaid/mermaid.min.js`

**Step 1: 创建目录并下载资源**

```bash
mkdir -p static/vendor/katex static/vendor/mermaid

# KaTeX
curl -L -o static/vendor/katex/katex.min.css https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css
curl -L -o static/vendor/katex/katex.min.js https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.js

# Mermaid
curl -L -o static/vendor/mermaid/mermaid.min.js https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js
```

**Step 2: 验证文件**

Run: `ls -la static/vendor/katex/` and `ls -la static/vendor/mermaid/`

**Step 3: Commit**

```bash
git add static/vendor/katex/ static/vendor/mermaid/
git commit -m "feat: add katex and mermaid frontend assets"
```

---

### Task 3: 创建自定义 CSS 样式文件

**Files:**
- Create: `static_src/css/highlight.css`
- Create: `static_src/css/main.css`

**Step 1: 创建目录**

```bash
mkdir -p static_src/css
```

**Step 2: 创建 static_src/css/highlight.css**

```css
/* Pygments 代码高亮样式 - GitHub 风格 */
.highlight {
    background: #f6f8fa;
    border-radius: 6px;
    padding: 16px;
    overflow-x: auto;
    font-size: 14px;
    line-height: 1.45;
}

.highlight pre {
    margin: 0;
    padding: 0;
    background: transparent;
}

.highlight .hll { background-color: #ffffcc }
.highlight .c { color: #6a737d; font-style: italic }
.highlight .err { color: #cb2431 }
.highlight .k { color: #d73a49; font-weight: bold }
.highlight .o { color: #24292e }
.highlight .cm { color: #6a737d; font-style: italic }
.highlight .cp { color: #6a737d }
.highlight .c1 { color: #6a737d; font-style: italic }
.highlight .cs { color: #6a737d; font-style: italic }
.highlight .gd { color: #cb2431; background-color: #ffeef0 }
.highlight .ge { font-style: italic }
.highlight .gh { color: #005cc5; font-weight: bold }
.highlight .gi { color: #22863a; background-color: #f0fff4 }
.highlight .gp { color: #6a737d }
.highlight .gs { font-weight: bold }
.highlight .gu { color: #6a737d }
.highlight .kc { color: #005cc5 }
.highlight .kd { color: #d73a49 }
.highlight .kn { color: #d73a49 }
.highlight .kp { color: #d73a49 }
.highlight .kr { color: #d73a49 }
.highlight .kt { color: #6f42c1 }
.highlight .m { color: #005cc5 }
.highlight .s { color: #032f62 }
.highlight .na { color: #6f42c1 }
.highlight .nb { color: #005cc5 }
.highlight .nc { color: #6f42c1 }
.highlight .no { color: #005cc5 }
.highlight .nd { color: #6f42c1 }
.highlight .ni { color: #24292e }
.highlight .ne { color: #d73a49 }
.highlight .nf { color: #6f42c1 }
.highlight .nl { color: #005cc5 }
.highlight .nn { color: #6f42c1 }
.highlight .nt { color: #22863a }
.highlight .nv { color: #e36209 }
.highlight .ow { color: #d73a49 }
.highlight .w { color: #bbbbbb }
.highlight .mb { color: #005cc5 }
.highlight .mf { color: #005cc5 }
.highlight .mh { color: #005cc5 }
.highlight .mi { color: #005cc5 }
.highlight .mo { color: #005cc5 }
.highlight .sb { color: #032f62 }
.highlight .sc { color: #032f62 }
.highlight .sd { color: #032f62 }
.highlight .s2 { color: #032f62 }
.highlight .se { color: #032f62 }
.highlight .sh { color: #032f62 }
.highlight .si { color: #005cc5 }
.highlight .sx { color: #032f62 }
.highlight .sr { color: #032f62 }
.highlight .s1 { color: #032f62 }
.highlight .ss { color: #005cc5 }
.highlight .bp { color: #005cc5 }
.highlight .vc { color: #e36209 }
.highlight .vg { color: #e36209 }
.highlight .vi { color: #e36209 }
.highlight .il { color: #005cc5 }
```

**Step 3: 创建 static_src/css/main.css**

```css
/* EDGP 静态博客主样式 */

/* 基础样式 */
:root {
    --primary-color: #667eea;
    --secondary-color: #764ba2;
    --text-color: #333;
    --text-muted: #6c757d;
    --bg-color: #fff;
    --border-color: #dee2e6;
}

body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    color: var(--text-color);
    line-height: 1.7;
}

/* 文章内容样式 */
.post-content {
    font-size: 16px;
    line-height: 1.8;
}

.post-content h1, .post-content h2, .post-content h3,
.post-content h4, .post-content h5, .post-content h6 {
    margin-top: 1.5em;
    margin-bottom: 0.5em;
    font-weight: 600;
    line-height: 1.3;
}

.post-content h1 { font-size: 2em; border-bottom: 1px solid var(--border-color); padding-bottom: 0.3em; }
.post-content h2 { font-size: 1.5em; border-bottom: 1px solid var(--border-color); padding-bottom: 0.3em; }
.post-content h3 { font-size: 1.25em; }
.post-content h4 { font-size: 1em; }

.post-content p {
    margin-bottom: 1em;
}

.post-content a {
    color: var(--primary-color);
    text-decoration: none;
}

.post-content a:hover {
    text-decoration: underline;
}

.post-content ul, .post-content ol {
    padding-left: 2em;
    margin-bottom: 1em;
}

.post-content blockquote {
    border-left: 4px solid var(--primary-color);
    padding-left: 1em;
    margin: 1em 0;
    color: var(--text-muted);
    background: #f8f9fa;
    padding: 1em;
    border-radius: 0 4px 4px 0;
}

.post-content table {
    width: 100%;
    border-collapse: collapse;
    margin: 1em 0;
}

.post-content th, .post-content td {
    border: 1px solid var(--border-color);
    padding: 8px 12px;
    text-align: left;
}

.post-content th {
    background: #f8f9fa;
    font-weight: 600;
}

.post-content img {
    max-width: 100%;
    height: auto;
    border-radius: 4px;
}

/* Mermaid 图表样式 */
.mermaid {
    text-align: center;
    margin: 1em 0;
    background: #f8f9fa;
    padding: 1em;
    border-radius: 4px;
}

/* KaTeX 公式样式 */
.katex-display {
    margin: 1em 0;
    text-align: center;
    overflow-x: auto;
}
```

**Step 4: 更新 settings.py 添加 STATIC_SRC_DIR**

已在 Phase 1 中配置。

**Step 5: Commit**

```bash
git add static_src/
git commit -m "feat: add custom css for code highlight and markdown content"
```

---

### Task 4: 更新 post.html 模板支持 mermaid 和 katex

**Files:**
- Modify: `site_templates/pages/post.html`

**Step 1: 更新 post.html**

```html
{% extends 'base.html' %}

{% block title %}{{ post.title }} - {{ site.title|default:'我的博客' }}{% endblock %}

{% block extra_head %}
<link rel="stylesheet" href="/static/vendor/katex/katex.min.css">
<link rel="stylesheet" href="/static/css/highlight.css">
<link rel="stylesheet" href="/static/css/main.css">
{% endblock %}

{% block content %}
<article class="post">
    <header class="post-header mb-4">
        <h1 class="display-5">{{ post.title }}</h1>
        <div class="text-muted">
            <time datetime="{{ post.published_at|date:'c' }}">
                {{ post.published_at|date:"Y年m月d日" }}
            </time>
            {% if post.category %}
            <span class="ms-2">
                <a href="/categories/{{ post.category.slug }}.html" class="text-decoration-none">
                    {{ post.category.name }}
                </a>
            </span>
            {% endif %}
            {% for tag in post.tags.all %}
            <span class="ms-2">
                <a href="/tags/{{ tag.slug }}.html" class="badge bg-secondary text-decoration-none">
                    #{{ tag.name }}
                </a>
            </span>
            {% endfor %}
        </div>
    </header>
    
    {% if post.cover_image %}
    <div class="post-cover mb-4">
        <img src="{{ post.cover_image.url }}" alt="{{ post.title }}" class="img-fluid rounded">
    </div>
    {% endif %}
    
    <div class="post-content">
        {{ post.content_html|safe|default:post.content|linebreaks }}
    </div>
    
    {% include 'components/prev_next.html' %}
</article>
{% endblock %}

{% block extra_scripts %}
<script src="/static/vendor/katex/katex.min.js"></script>
<script src="/static/vendor/mermaid/mermaid.min.js"></script>
<script>
    // 初始化 Mermaid
    if (typeof mermaid !== 'undefined') {
        mermaid.initialize({ startOnLoad: true, theme: 'default' });
    }
    
    // 渲染 KaTeX 公式
    document.querySelectorAll('.katex-display').forEach(function(el) {
        if (typeof katex !== 'undefined') {
            katex.render(el.textContent, el, { displayMode: true, throwOnError: false });
        }
    });
</script>
{% endblock %}
```

**Step 2: Commit**

```bash
git add site_templates/pages/post.html
git commit -m "feat: update post template with mermaid and katex support"
```

---

## Phase 2.2: 静态页面生成器

### Task 5: 创建 StaticSiteGenerator 类

**Files:**
- Create: `blog/utils/generator.py`

**Step 1: 创建 generator.py**

```python
"""
静态站点生成器
将数据库内容渲染为静态 HTML 文件
"""
import os
import shutil
from pathlib import Path
from django.conf import settings
from django.template.loader import render_to_string
from django.utils import timezone


class StaticSiteGenerator:
    """静态站点生成器"""
    
    def __init__(self):
        self.output_dir = Path(settings.OUTPUT_DIR)
        self.templates_dir = Path(settings.SITE_TEMPLATES_DIR)
        self.static_src_dir = Path(settings.STATIC_SRC_DIR)
        self.site_config = self._load_site_config()
    
    def _load_site_config(self):
        """加载站点配置"""
        from ..models import SiteConfig
        
        return {
            'title': SiteConfig.get('site.title', '我的博客'),
            'description': SiteConfig.get('site.description', ''),
            'author': SiteConfig.get('author.name', ''),
            'url': SiteConfig.get('site.url', ''),
        }
    
    def _get_context(self, extra=None):
        """获取通用模板上下文"""
        context = {'site': self.site_config}
        if extra:
            context.update(extra)
        return context
    
    def _write_page(self, relative_path, content):
        """写入页面文件"""
        file_path = self.output_dir / relative_path
        file_path.parent.mkdir(parents=True, exist_ok=True)
        file_path.write_text(content, encoding='utf-8')
        print(f"  生成: {relative_path}")
    
    def _clean_output(self):
        """清理输出目录（保留 .git）"""
        print("🧹 清理输出目录...")
        for item in self.output_dir.iterdir():
            if item.name != '.git':
                if item.is_dir():
                    shutil.rmtree(item)
                else:
                    item.unlink()
    
    def _copy_static(self):
        """复制静态资源到输出目录"""
        print("📦 复制静态资源...")
        
        # 复制 static_src 到 output/static
        dest = self.output_dir / 'static'
        if dest.exists():
            shutil.rmtree(dest)
        
        if self.static_src_dir.exists():
            shutil.copytree(self.static_src_dir, dest)
        
        # 复制 vendor 资源
        vendor_src = Path(settings.STATICFILES_DIRS[0]) / 'vendor'
        vendor_dest = dest / 'vendor'
        if vendor_src.exists():
            if vendor_dest.exists():
                shutil.rmtree(vendor_dest)
            shutil.copytree(vendor_src, vendor_dest)
        
        print("  静态资源复制完成")
    
    def generate_all(self):
        """生成全部静态页面"""
        print("🚀 开始生成静态站点...")
        
        self._clean_output()
        self._copy_static()
        
        self._generate_index()
        self._generate_posts()
        self._generate_archive()
        self._generate_tags()
        self._generate_categories()
        
        print("✅ 静态站点生成完成！")
    
    def _generate_index(self):
        """生成首页"""
        from ..models import Post, Category, Tag
        
        print("📄 生成首页...")
        posts = Post.objects.filter(status='published', post_type='post').order_by('-published_at')[:10]
        categories = Category.objects.all()
        tags = Tag.objects.all()
        
        context = self._get_context({
            'posts': posts,
            'categories': categories,
            'tags': tags,
        })
        
        html = render_to_string('pages/index.html', context)
        self._write_page('index.html', html)
    
    def _generate_posts(self):
        """生成所有博文页面"""
        from ..models import Post
        
        print("📝 生成博文页面...")
        posts = list(Post.objects.filter(status='published', post_type='post').order_by('-published_at'))
        
        for i, post in enumerate(posts):
            prev_post = posts[i - 1] if i > 0 else None
            next_post = posts[i + 1] if i < len(posts) - 1 else None
            
            context = self._get_context({
                'post': post,
                'prev_post': prev_post,
                'next_post': next_post,
            })
            
            html = render_to_string('pages/post.html', context)
            
            year = post.published_at.year if post.published_at else post.created_at.year
            path = f'posts/{year}/{post.slug}.html'
            self._write_page(path, html)
    
    def _generate_archive(self):
        """生成归档页"""
        from ..models import Post
        
        print("📚 生成归档页...")
        posts = Post.objects.filter(status='published', post_type='post').order_by('-published_at')
        
        context = self._get_context({'posts': posts})
        html = render_to_string('pages/archive.html', context)
        self._write_page('archive.html', html)
    
    def _generate_tags(self):
        """生成标签页"""
        from ..models import Tag, Post
        
        print("🏷️ 生成标签页...")
        
        # 生成标签列表页
        tags = Tag.objects.all()
        context = self._get_context({'tags': tags})
        html = render_to_string('pages/tags.html', context)
        self._write_page('tags/index.html', html)
        
        # 生成每个标签页
        for tag in tags:
            posts = Post.objects.filter(status='published', tags=tag)
            context = self._get_context({'tag': tag, 'posts': posts})
            html = render_to_string('pages/tag.html', context)
            self._write_page(f'tags/{tag.slug}.html', html)
    
    def _generate_categories(self):
        """生成分类页"""
        from ..models import Category, Post
        
        print("📁 生成分类页...")
        
        for cat in Category.objects.all():
            posts = Post.objects.filter(status='published', category=cat)
            context = self._get_context({'category': cat, 'posts': posts})
            html = render_to_string('pages/category.html', context)
            self._write_page(f'categories/{cat.slug}.html', html)
```

**Step 2: 更新 blog/utils/__init__.py**

```python
"""
Blog utility functions.
"""
from .markdown import render_markdown, MarkdownRenderer
from .generator import StaticSiteGenerator

__all__ = ['render_markdown', 'MarkdownRenderer', 'StaticSiteGenerator']
```

**Step 3: Commit**

```bash
git add blog/utils/
git commit -m "feat: add StaticSiteGenerator for static page generation"
```

---

### Task 6: 创建 generate 管理命令

**Files:**
- Create: `blog/management/commands/generate.py`

**Step 1: 创建 generate.py**

```python
"""
生成静态站点命令
用法: python manage.py generate
"""
from django.core.management.base import BaseCommand
from blog.utils.generator import StaticSiteGenerator


class Command(BaseCommand):
    help = '生成静态博客站点'
    
    def add_arguments(self, parser):
        parser.add_argument(
            '--clean',
            action='store_true',
            help='清理输出目录后重新生成',
        )
    
    def handle(self, *args, **options):
        try:
            generator = StaticSiteGenerator()
            generator.generate_all()
            self.stdout.write(self.style.SUCCESS('✅ 静态站点生成成功！'))
        except Exception as e:
            self.stdout.write(self.style.ERROR(f'❌ 生成失败: {str(e)}'))
            raise
```

**Step 2: 验证命令**

Run: `python manage.py generate --help`
Expected: 显示命令帮助信息

**Step 3: Commit**

```bash
git add blog/management/commands/generate.py
git commit -m "feat: add generate management command"
```

---

### Task 7: 创建缺失的模板文件

**Files:**
- Create: `site_templates/pages/archive.html`
- Create: `site_templates/pages/tags.html`
- Create: `site_templates/pages/tag.html`
- Create: `site_templates/pages/category.html`

**Step 1: 创建 archive.html**

```html
{% extends 'base.html' %}

{% block title %}归档 - {{ site.title }}{% endblock %}

{% block extra_head %}
<link rel="stylesheet" href="/static/css/main.css">
{% endblock %}

{% block content %}
<h1 class="mb-4">归档</h1>

{% regroup posts by published_at|date:"Y" as posts_by_year %}

{% for year in posts_by_year %}
<h2 class="mt-4 mb-3">{{ year.grouper }}</h2>
<ul class="list-unstyled">
    {% for post in year.list %}
    <li class="mb-2 d-flex justify-content-between align-items-center">
        <span>{{ post.published_at|date:"m-d" }}</span>
        <a href="/posts/{{ post.published_at.year }}/{{ post.slug }}.html">{{ post.title }}</a>
    </li>
    {% endfor %}
</ul>
{% empty %}
<p class="text-muted">暂无文章</p>
{% endfor %}
{% endblock %}
```

**Step 2: 创建 tags.html**

```html
{% extends 'base.html' %}

{% block title %}标签 - {{ site.title }}{% endblock %}

{% block content %}
<h1 class="mb-4">标签</h1>

<div class="d-flex flex-wrap gap-2">
{% for tag in tags %}
<a href="/tags/{{ tag.slug }}.html" class="btn btn-outline-secondary">
    {{ tag.name }} <span class="badge bg-secondary">{{ tag.post_count }}</span>
</a>
{% empty %}
<p class="text-muted">暂无标签</p>
{% endfor %}
</div>
{% endblock %}
```

**Step 3: 创建 tag.html**

```html
{% extends 'base.html' %}

{% block title %}#{{ tag.name }} - {{ site.title }}{% endblock %}

{% block content %}
<h1 class="mb-4">#{{ tag.name }}</h1>

{% for post in posts %}
<article class="card mb-3">
    <div class="card-body">
        <h2 class="card-title h5">
            <a href="/posts/{{ post.published_at.year }}/{{ post.slug }}.html">{{ post.title }}</a>
        </h2>
        <p class="card-text text-muted">{{ post.published_at|date:"Y-m-d" }}</p>
    </div>
</article>
{% empty %}
<p class="text-muted">该标签下暂无文章</p>
{% endfor %}
{% endblock %}
```

**Step 4: 创建 category.html**

```html
{% extends 'base.html' %}

{% block title %}{{ category.name }} - {{ site.title }}{% endblock %}

{% block content %}
<h1 class="mb-4">{{ category.name }}</h1>
{% if category.description %}
<p class="text-muted mb-4">{{ category.description }}</p>
{% endif %}

{% for post in posts %}
<article class="card mb-3">
    <div class="card-body">
        <h2 class="card-title h5">
            <a href="/posts/{{ post.published_at.year }}/{{ post.slug }}.html">{{ post.title }}</a>
        </h2>
        <p class="card-text text-muted">{{ post.published_at|date:"Y-m-d" }}</p>
    </div>
</article>
{% empty %}
<p class="text-muted">该分类下暂无文章</p>
{% endfor %}
{% endblock %}
```

**Step 5: Commit**

```bash
git add site_templates/pages/
git commit -m "feat: add archive, tags, tag, category templates"
```

---

### Task 8: 更新 Post 模型自动渲染 Markdown

**Files:**
- Modify: `blog/models.py`

**Step 1: 更新 models.py 中的 Post.save 方法**

在 Post 类的 save 方法中添加 Markdown 渲染：

```python
from blog.utils import render_markdown

class Post(models.Model):
    # ... 现有代码 ...
    
    def save(self, *args, **kwargs):
        if not self.slug:
            self.slug = slugify(self.title)
        
        # 自动渲染 Markdown
        if self.content:
            self.content_html = render_markdown(self.content)
        
        # 发布时自动设置发布时间
        if self.status == 'published' and not self.published_at:
            self.published_at = timezone.now()
        
        super().save(*args, **kwargs)
```

**Step 2: 需要在文件顶部添加导入**

```python
from django.utils import timezone
from django.utils.text import slugify
```

**Step 3: Commit**

```bash
git add blog/models.py
git commit -m "feat: auto render markdown on post save"
```

---

### Task 9: 更新 settings.py 添加 STATIC_SRC_DIR

**Files:**
- Modify: `edgp/settings.py`

**Step 1: 确认 STATIC_SRC_DIR 已配置**

检查 settings.py 中是否有：
```python
STATIC_SRC_DIR = BASE_DIR / "static_src"
```

如果没有，添加到自定义设置区域。

**Step 2: 添加 STATICFILES_DIRS**

确保 STATICFILES_DIRS 包含 static_src：
```python
STATICFILES_DIRS = [
    BASE_DIR / "static",
    BASE_DIR / "static_src",
]
```

**Step 3: Commit**

```bash
git add edgp/settings.py
git commit -m "feat: configure static_src in settings"
```

---

### Task 10: 测试静态生成器

**Step 1: 创建测试数据**

通过 Admin 创建：
1. 分类：技术
2. 标签：Python, Django
3. 博文：测试文章（状态：已发布）

**Step 2: 运行生成命令**

Run: `python manage.py generate`

Expected output:
```
🚀 开始生成静态站点...
🧹 清理输出目录...
📦 复制静态资源...
📄 生成首页...
📝 生成博文页面...
📚 生成归档页...
🏷️ 生成标签页...
📁 生成分类页...
✅ 静态站点生成成功！
```

**Step 3: 检查输出目录**

Run: `ls -la output/`
Expected: 看到 index.html, posts/, tags/, categories/ 等文件

**Step 4: 验证生成的 HTML**

Run: `cat output/index.html`
Expected: 包含渲染后的 HTML 内容

---

## Phase 2.3: 完整数据模型

### Task 11: 添加 Moment 模型

**Files:**
- Modify: `blog/models.py`
- Create: migration

**Step 1: 在 models.py 中添加 Moment 模型**

```python
class Moment(models.Model):
    """说说 - 简短随笔"""
    content = models.TextField('内容 (Markdown)')
    content_html = models.TextField('渲染后HTML', blank=True)
    images = models.JSONField('图片列表', default=list, blank=True)
    created_at = models.DateTimeField('创建时间', auto_now_add=True)
    
    class Meta:
        verbose_name = '说说'
        verbose_name_plural = '说说'
        ordering = ['-created_at']
    
    def __str__(self):
        return self.content[:50] + '...' if len(self.content) > 50 else self.content
    
    def save(self, *args, **kwargs):
        from blog.utils import render_markdown
        if self.content:
            self.content_html = render_markdown(self.content)
        super().save(*args, **kwargs)
```

**Step 2: 创建迁移**

Run: `python manage.py makemigrations blog`
Run: `python manage.py migrate`

**Step 3: Commit**

```bash
git add blog/models.py blog/migrations/
git commit -m "feat: add Moment model for short posts"
```

---

### Task 12: 添加 FriendLink 模型

**Files:**
- Modify: `blog/models.py`
- Create: migration

**Step 1: 在 models.py 中添加 FriendLink 模型**

```python
class FriendLink(models.Model):
    """友情链接"""
    name = models.CharField('名称', max_length=100)
    url = models.URLField('链接地址')
    description = models.TextField('描述', blank=True)
    avatar = models.URLField('头像URL', blank=True)
    order = models.IntegerField('排序', default=0)
    is_active = models.BooleanField('是否显示', default=True)
    
    class Meta:
        verbose_name = '友链'
        verbose_name_plural = '友链'
        ordering = ['order', 'name']
    
    def __str__(self):
        return self.name
```

**Step 2: 创建迁移并应用**

Run: `python manage.py makemigrations blog`
Run: `python manage.py migrate`

**Step 3: Commit**

```bash
git add blog/models.py blog/migrations/
git commit -m "feat: add FriendLink model"
```

---

### Task 13: 添加 SiteConfig 模型

**Files:**
- Modify: `blog/models.py`
- Create: migration

**Step 1: 在 models.py 中添加 SiteConfig 模型**

```python
class SiteConfig(models.Model):
    """站点配置 - 键值对存储"""
    key = models.CharField('配置键', max_length=50, unique=True)
    value = models.TextField('配置值')
    description = models.CharField('说明', max_length=200, blank=True)
    
    class Meta:
        verbose_name = '站点配置'
        verbose_name_plural = '站点配置'
    
    def __str__(self):
        return self.key
    
    @classmethod
    def get(cls, key, default=''):
        """获取配置值"""
        try:
            return cls.objects.get(key=key).value
        except cls.DoesNotExist:
            return default
    
    @classmethod
    def set(cls, key, value, description=''):
        """设置配置值"""
        obj, _ = cls.objects.update_or_create(
            key=key,
            defaults={'value': value, 'description': description}
        )
        return obj
```

**Step 2: 创建迁移并应用**

Run: `python manage.py makemigrations blog`
Run: `python manage.py migrate`

**Step 3: Commit**

```bash
git add blog/models.py blog/migrations/
git commit -m "feat: add SiteConfig model for site settings"
```

---

## Phase 2.4: Admin 增强

### Task 14: 注册新模型到 Admin

**Files:**
- Modify: `blog/admin.py`

**Step 1: 更新 admin.py**

```python
from django.contrib import admin
from django.utils.html import format_html
from .models import Post, Category, Tag, Moment, FriendLink, SiteConfig


# ... 现有的 CategoryAdmin, TagAdmin, PostAdmin ...


@admin.register(Moment)
class MomentAdmin(admin.ModelAdmin):
    list_display = ['id', 'content_preview', 'created_at']
    list_filter = ['created_at']
    
    def content_preview(self, obj):
        return obj.content[:100] + '...' if len(obj.content) > 100 else obj.content
    content_preview.short_description = '内容预览'


@admin.register(FriendLink)
class FriendLinkAdmin(admin.ModelAdmin):
    list_display = ['name', 'url', 'order', 'is_active', 'avatar_preview']
    list_editable = ['order', 'is_active']
    search_fields = ['name', 'url']
    
    def avatar_preview(self, obj):
        if obj.avatar:
            return format_html('<img src="{}" width="40" height="40" style="border-radius:50%;">', obj.avatar)
        return '-'
    avatar_preview.short_description = '头像'


@admin.register(SiteConfig)
class SiteConfigAdmin(admin.ModelAdmin):
    list_display = ['key', 'value_preview', 'description']
    search_fields = ['key', 'description']
    
    def value_preview(self, obj):
        return obj.value[:50] + '...' if len(obj.value) > 50 else obj.value
    value_preview.short_description = '值'
```

**Step 2: Commit**

```bash
git add blog/admin.py
git commit -m "feat: register Moment, FriendLink, SiteConfig in admin"
```

---

### Task 15: 添加 Admin 自定义 Action - 重新生成

**Files:**
- Modify: `blog/admin.py`

**Step 1: 在 admin.py 中添加自定义 Action**

```python
from django.contrib import messages
from blog.utils.generator import StaticSiteGenerator


def regenerate_static(modeladmin, request, queryset):
    """重新生成静态页面"""
    try:
        generator = StaticSiteGenerator()
        generator.generate_all()
        messages.success(request, '✅ 静态页面生成成功！')
    except Exception as e:
        messages.error(request, f'❌ 生成失败: {str(e)}')

regenerate_static.short_description = '🔄 重新生成静态页面'


# 在任意 ModelAdmin 中添加
class PostAdmin(admin.ModelAdmin):
    actions = [regenerate_static]
    # ... 现有代码 ...
```

**Step 2: Commit**

```bash
git add blog/admin.py
git commit -m "feat: add regenerate_static admin action"
```

---

## Task 16: 最终验证

**Step 1: 运行系统检查**

Run: `python manage.py check`
Expected: 无问题

**Step 2: 创建测试数据**

通过 Admin 创建：
- 2 个分类
- 3 个标签
- 3 篇博文（已发布）
- 1 条说说
- 2 个友链
- 站点配置（site.title, site.description）

**Step 3: 运行生成器**

Run: `python manage.py generate`

**Step 4: 检查输出目录结构**

Run: `find output -type f -name "*.html"`

Expected:
```
output/index.html
output/archive.html
output/posts/2026/*.html
output/tags/index.html
output/tags/*.html
output/categories/*.html
output/static/
```

**Step 5: 最终 Commit**

```bash
git add .
git commit -m "feat: complete Phase 2 - core functionality ready"
```

---

## Phase 2 完成标志

- [ ] Markdown 渲染器支持 mermaid、LaTeX、代码高亮
- [ ] 静态生成器可生成完整站点
- [ ] Moment, FriendLink, SiteConfig 模型可用
- [ ] Admin 可管理所有模型
- [ ] `python manage.py generate` 生成静态文件到 output/
- [ ] 生成的 HTML 包含正确的 CSS/JS 引用

---

## 后续阶段

| 阶段 | 内容 |
|------|------|
| Phase 2.5 | 前端 UI 设计 (美化模板) |
| Phase 3 | 完善与部署 (Git push, 说说页, 友链页, 关于页) |